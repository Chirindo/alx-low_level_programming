#include "main.h"

/**
 * jack_bauer - prints minutes
 *
 * Return: no return
 */

void jack_bauer(void)
{
	int a, b,

	for (a = 0; a<= 24; a++)
	{
		for (b = 0; b < 60; b++)
		{
			if (a < 10)
			{
				_putchar('0')
				_putchar(a + '0')
			}
			if (b 
			_putchar(a);
			_putchar(':');
			_putchar(b);
		}
	}
}
